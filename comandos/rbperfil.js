const Discord = require("discord.js");

module.exports.run = async (bot, message, args) => {
    
    message.channel.send(`${message.author}, :x: | Comando indisponivel.`)
}
    module.exports.help = {
    name: "rbperfil"
  }